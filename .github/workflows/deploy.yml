name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Inject data.json from secret
        run: echo '${{ secrets.DATA_JSON }}' > src/data.json
      - name: Inject .env from secret
        run: echo '${{ secrets.DOTENV }}' > .env
      - run: npm run build
      - name: Move build to subdirectory
        run: |
          mkdir -p deploy/wedding/baeleem
          cp -r dist/* deploy/wedding/baeleem/
          cp public/CNAME deploy/CNAME
      - uses: actions/upload-pages-artifact@v3
        with:
          path: deploy

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
